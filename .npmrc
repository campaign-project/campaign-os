# Hoisted (npm-like flat) node_modules. Required by React Native / Expo / Metro, whose
# resolvers don't traverse pnpm's default isolated symlink tree (apps/mobile, RFC-002).
# Functionally transparent to the Next.js web app; reversible by removing this line + reinstalling.
node-linker=hoisted
